# pocket-av-app

A Flutter audio player app with a focus on internet radio.

## Getting Started

This is a minimal scaffold. To generate the full platform directories:

```bash
flutter create --project-name pocket_av_app .
```

Then run the app:

```bash
flutter run
```
